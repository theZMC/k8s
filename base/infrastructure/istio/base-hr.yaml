apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: base
spec:
  releaseName: base
  interval: 1h
  chart:
    spec:
      chart: base
      version: 1.27.3
      sourceRef:
        kind: HelmRepository
        name: istio
