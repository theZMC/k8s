apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ztunnel
  namespace: istio-system
spec:
  releaseName: ztunnel
  interval: 15m
  chart:
    spec:
      chart: ztunnel
      version: 1.27.2
      sourceRef:
        kind: HelmRepository
        name: istio
        namespace: istio-system
  dependsOn:
  - name: cni
    namespace: istio-system
